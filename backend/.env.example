# =======================================================
# BACKEND ENVIRONMENT VARIABLES
# COPY this file to a new file named '.env' and fill in your actual credentials.
# =======================================================

# --- SERVER CONFIGURATION ---

# The port on which the Node/Express server will listen
PORT=5000

# --- DATABASE CONFIGURATION (MySQL) ---

# The host where your MySQL server is running (usually 127.0.0.1 or localhost)
DB_HOST=localhost

# Your MySQL user account. For local development, this is often 'root'
DB_USER=root

# The password for your specified DB_USER
DB_PASSWORD=

# The name of the MySQL database this application connects to
DB_NAME=lms_system


# --- APPLICATION SECRETS ---

# A long random string used for signing JSON Web Tokens (JWT)
# MUST be kept secret and should be at least 32 characters long
# Generate using: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=your_long_random_jwt_secret_min_32_chars

# Express session secret for OAuth
# Generate using: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
SESSION_SECRET=your_long_random_session_secret_min_32_chars

# --- ENVIRONMENT ---
# Set to 'production' when deploying to enable security features
NODE_ENV=development

# --- CORS CONFIGURATION ---
# The allowed frontend origin for browser requests
# Example for Vite dev server: http://localhost:5173
FRONTEND_ORIGIN=http://localhost:5173

# --- EMAIL SERVICE CONFIGURATION (For OTP, Password Reset, etc.) ---

EMAIL_USER=
EMAIL_PASS=

# --- GITHUB OAUTH ---
# Create an OAuth App at https://github.com/settings/developers
# Authorization callback URL should be: http://localhost:5000/api/auth/github/callback (adjust for prod)
GITHUB_CLIENT_ID=
GITHUB_CLIENT_SECRET=
GITHUB_CALLBACK_URL=http://localhost:5000/api/auth/github/callback