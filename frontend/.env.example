# --- FRONTEND ENVIRONMENT VARIABLES (VITE) ---

# This variable defines the root URL for all API requests from the React application.
# Teammates should set this to the address and port where their local backend server is running.
VITE_API_URL=http://localhost:5000/api